diff --git a/dist/component/invitations.js b/dist/component/invitations.js
index cdeec9ef0c4c3e6b1f256685f02d2c49c5469f2f..a2da412b062eb31c7feb2506960fcc5620454c9a 100644
--- a/dist/component/invitations.js
+++ b/dist/component/invitations.js
@@ -1,3 +1,5 @@
+import { paginator } from "convex-helpers/server/pagination";
+import schema from "./schema.js";
 import { paginationOptsValidator } from "convex/server";
 import { v } from "convex/values";
 import { mutation, query } from "./_generated/server.js";
@@ -368,7 +370,7 @@ export const listByResource = query({
         assertString(args.scope, { max: 256 });
         assertString(args.resourceRef, { max: 512 });
         assertLimit(args.paginationOpts.numItems);
-        const result = await ctx.db
+        const result = await paginator(ctx.db, schema)
             .query("invitations")
             .withIndex("by_scope_resource_state_created", (q) => q
             .eq("scope", args.scope)
@@ -389,7 +391,7 @@ export const listByState = query({
     handler: async (ctx, args) => {
         assertString(args.scope, { max: 256 });
         assertLimit(args.paginationOpts.numItems);
-        const result = await ctx.db
+        const result = await paginator(ctx.db, schema)
             .query("invitations")
             .withIndex("by_scope_state_created", (q) => q.eq("scope", args.scope).eq("state", args.state))
             .order("desc")
@@ -408,7 +410,7 @@ export const listPendingByAudience = query({
         assertString(args.scope, { max: 256 });
         assertString(args.audienceRef, { max: 512 });
         assertLimit(args.paginationOpts.numItems);
-        const result = await ctx.db
+        const result = await paginator(ctx.db, schema)
             .query("invitations")
             .withIndex("by_scope_audience_state_created", (q) => q
             .eq("scope", args.scope)
diff --git a/package.json b/package.json
index 6382774f7ef059204ce4daab002ea0638d46fa50..880f15eae0d2a8f379f7b184890dda42615b1487 100644
--- a/package.json
+++ b/package.json
@@ -85,5 +85,8 @@
     "typescript": "7.0.2",
     "vite": "8.2.1",
     "vitest": "4.1.10"
+  },
+  "dependencies": {
+    "convex-helpers": "^0.1.124"
   }
 }
diff --git a/src/component/invitations.ts b/src/component/invitations.ts
index daafe6337d94e5516fd4cfe99f4c4ab35ad0b6e2..ccbac1ee21c1c881a6d20cad0801a7dbaa1d57b0 100644
--- a/src/component/invitations.ts
+++ b/src/component/invitations.ts
@@ -1,3 +1,5 @@
+import { paginator } from "convex-helpers/server/pagination";
+import schema from "./schema.js";
 import { paginationOptsValidator } from "convex/server";
 import { v } from "convex/values";
 import type { Doc, Id } from "./_generated/dataModel.js";
@@ -401,7 +403,7 @@ export const listByResource = query({
     assertString(args.scope, { max: 256 });
     assertString(args.resourceRef, { max: 512 });
     assertLimit(args.paginationOpts.numItems);
-    const result = await ctx.db
+    const result = await paginator(ctx.db, schema)
       .query("invitations")
       .withIndex("by_scope_resource_state_created", (q) =>
         q
@@ -425,7 +427,7 @@ export const listByState = query({
   handler: async (ctx, args) => {
     assertString(args.scope, { max: 256 });
     assertLimit(args.paginationOpts.numItems);
-    const result = await ctx.db
+    const result = await paginator(ctx.db, schema)
       .query("invitations")
       .withIndex("by_scope_state_created", (q) =>
         q.eq("scope", args.scope).eq("state", args.state),
@@ -447,7 +449,7 @@ export const listPendingByAudience = query({
     assertString(args.scope, { max: 256 });
     assertString(args.audienceRef, { max: 512 });
     assertLimit(args.paginationOpts.numItems);
-    const result = await ctx.db
+    const result = await paginator(ctx.db, schema)
       .query("invitations")
       .withIndex("by_scope_audience_state_created", (q) =>
         q
